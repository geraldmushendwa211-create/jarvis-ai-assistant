# ============================================================
# JARVIS AI Assistant — Python dependencies
# Install with:  pip install -r requirements.txt
# Requires Python 3.10+ and (on Linux) the tkinter system package.
# See docs/SETUP.md for full setup instructions.
# ============================================================

# ---- AI brain ----
google-genai>=1.0.0          # Gemini SDK (from google import genai)
python-dotenv>=1.0.0         # loads GEMINI_API_KEY from .env

# ---- Voice: listen ----
SpeechRecognition>=3.10.0    # speech-to-text (Google backend)
sounddevice>=0.5.0           # microphone recording
numpy>=1.26.0                # audio buffers
scipy>=1.11.0                # wav file writing

# ---- Voice: speak ----
edge-tts>=7.0.0              # natural neural TTS (British Ryan voice)
playsound3>=3.3.0            # mp3 playback (Linux needs GStreamer, see SETUP.md)

# ---- Video pipeline ----
moviepy>=2.0.0               # video assembly (v2 API: resized/subclipped/with_audio)
faster-whisper>=1.0.0        # word-level transcription for captions + trim
imageio-ffmpeg>=0.5.0        # bundles an ffmpeg binary (no system ffmpeg needed)
pydub>=0.25.1                # silence trimming on voiceovers
pillow>=10.0.0               # image handling (moviepy dependency)
imageio>=2.30.0              # video I/O (moviepy dependency)
tqdm>=4.60.0                 # progress bars (moviepy/whisper)
requests>=2.30.0             # HTTP (used by several of the above)
